#!/bin/bash
if _command_exists 'networksetup'; then
    wifi="`networksetup -getairportnetwork en0`"
    if [[ $(echo $wifi | grep -e ':') ]] ; then
        echo ${wifi#*: }
    else
        echo No Connection
    fi
fi
exit 0
