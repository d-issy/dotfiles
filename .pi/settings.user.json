{
  "tools": {
    "lint": {
      "description": "Run all lint and type checks for the project (no files are modified).",
      "executionMode": "parallel",
      "promptSnippet": "Run lint and type checks for the project",
      "promptGuidelines": [
        "Use lint after editing files to verify; it runs the same checks as CI and modifies nothing."
      ],
      "commands": [
        {
          "label": "tsgo",
          "command": "pnpm",
          "arguments": [
            "lint"
          ]
        },
        {
          "label": "oxlint",
          "command": "oxlint",
          "arguments": [
            "--deny-warnings"
          ]
        },
        {
          "label": "deadnix",
          "command": "deadnix",
          "arguments": [
            "--fail",
            "."
          ]
        },
        {
          "label": "statix",
          "command": "statix",
          "arguments": [
            "check",
            "."
          ]
        }
      ]
    },
    "format": {
      "description": "Format the project in place with treefmt.",
      "executionMode": "sequential",
      "promptSnippet": "Format the project in place",
      "promptGuidelines": [
        "Use format to auto-fix formatting, then run lint to verify."
      ],
      "commands": [
        {
          "label": "treefmt",
          "command": "treefmt"
        }
      ]
    },
    "homemanager_switch": {
      "description": "Run Home Manager switch for the current host via the flake.",
      "executionMode": "sequential",
      "commands": [
        {
          "label": "home-manager switch",
          "command": "nix",
          "arguments": [
            "run",
            ".#switch"
          ],
          "timeoutSeconds": 600
        }
      ]
    }
  },
  "focuses": {
    "dotfiles": {
      "description": "Run dotfiles project formatting and lint checks.",
      "prompt": "You are in dotfiles focus. Use project tools for formatting and linting when appropriate.",
      "tools": [
        "format",
        "lint"
      ],
      "transition": "auto",
      "color": "accent"
    },
    "switch": {
      "description": "Run Home Manager switch for this dotfiles project.",
      "prompt": "You are in switch focus. Use homemanager_switch when the user asks to apply the Home Manager configuration.",
      "tools": [
        "homemanager_switch"
      ],
      "transition": "confirm",
      "color": "caution"
    }
  }
}
